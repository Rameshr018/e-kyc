{
  "name": "myproject",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:backend": "set PYTHONPATH=%cd%\\backend && uvicorn app.main:app --reload --app-dir backend",

    "start:frontend": "cd frontend2 && npm run dev",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
